import { style } from '@vanilla-extract/css';

export const hashTag = style({
  display: 'flex',
  gap: '0.6rem',
});

export const hashTagItem = style({
  backgroundColor: '#F7F7F7',
  padding: '0.3rem 0.6rem',
  borderRadius:"0.4rem",
  opacity:'50%'
});
